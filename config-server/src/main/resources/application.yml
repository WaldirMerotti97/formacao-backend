spring:
  application:
    name: 'config-server'
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_SERVER_URI:https://github.com/WaldirMerotti97/formacao-backend-expert-configs}
          default-label: main
          username: ${CONFIG_SERVER_USERNAME}
          password: ${CONFIG_SERVER_PASSWORD}

server:
  port: ${PORT:8888}